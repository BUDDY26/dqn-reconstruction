# Runtime dependencies for the DQN reconstruction.
# Install with: pip install -r requirements.txt
#
# Annotations:
#   [CONFIRMED USE]  Required by a confirmed component (e.g. StandardScaler, Gymnasium)
#   [FIRST PASS]     Added in the first coding pass (config, data, env)
#   [NEXT PASS]      Needed for network/agent/training (not yet implemented)

# Data and preprocessing  [CONFIRMED USE, FIRST PASS]
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0       # StandardScaler [CONFIRMED]

# Reinforcement learning environment  [CONFIRMED USE, FIRST PASS]
gymnasium>=0.29.0          # Gymnasium.Env interface

# Deep learning  [CONFIRMED USE, SECOND PASS — network.py, agent.py, train.py]
torch>=2.0.0

# Testing
pytest>=7.4.0
pytest-cov>=7.1.0
